# 일일 개발 로그

> 날짜별 구현 이력을 보존하는 문서다. 현재 상태 판단에는 [DEVELOPMENT_HANDOFF.md](DEVELOPMENT_HANDOFF.md)를 사용한다.

## 2026-07-09

### 오늘의 목표

실제 API 연동 중심의 정책 추천 Agent MVP를 만들고, 팀원이 공유한 GitHub 구조와 현재 로컬 작업물을 통합해 GitHub 업데이트 가능한 상태로 정리한다.

### 진행한 작업

- 프로젝트 폴더에 `policy-compass` 개발 폴더를 생성했다.
- 제공된 스타터 코드를 기반으로 정책 추천 Agent 방향을 잡았다.
- 의료 QA Agent 스타터 코드를 정책 추천 도메인으로 전환했다.
- 사용자 조건 분석, 부족 조건 확인, 정책 검색, 점수화, 응답 생성 흐름을 1차 구현했다.
- 고용24 국민내일배움카드 훈련과정 API를 핵심 실데이터 소스로 연결했다.
- 기업마당 API 인증키를 `BIZINFO_API_KEY`로 받아 실제 API를 호출하는 구조를 추가했다.
- API 호출 실패 또는 키 누락 시 데모용 대체 정책 데이터로 보완하지 않고 빈 결과 또는 안내 fallback으로 드러나도록 구현했다.
- 팀원이 공유한 `policy-compass-agent` zip 구조를 검토했다.
- 팀원 구조의 장점인 `app/api/routes`, `app/graph`, `app/repositories`, `app/schemas`, `app/static`, `tests` 구조를 현재 프로젝트에 통합했다.
- 배포/연동 테스트 기준으로 데모용 정책 JSON 의존을 제거했다.
- 기업마당 API 응답을 팀원 스키마로 직접 정규화하는 로직을 `app/repositories/policy.py`에 반영했다.
- FastAPI 단일 서버 구조로 정리하고 실행 포트를 8000으로 통일했다.
- 기존 `/health`, `/api/v1/chat/sync`, `/api/v1/chat` 호환 라우트를 유지했다.
- 기존 Streamlit 8002 구조를 제거하고, FastAPI 정적 UI(`/`) 기준으로 정리했다.
- `pyproject.toml`, `uv.lock`, `.env.example`, `Dockerfile`, `start.sh`, README를 통합 구조 기준으로 갱신했다.
- 개발 현황, 협업 규칙, 로드맵, 다음 작업, 배포 메모 문서를 현재 상태에 맞게 갱신했다.

### 현재 완료된 것

- FastAPI 단일 서버 실행 구조
- 정적 채팅 UI: `http://localhost:8000/`
- API 문서: `http://localhost:8000/docs`
- LangGraph Agent 구조
- 세션 기반 프로필 누적
- LLM 사용 가능 + 규칙 기반 fallback 구조
- 고용24 훈련과정 실데이터 기반 데모
- 기업마당 API 호출 및 정규화 1차 구현
- 정책 목록/상세/검색 API
- SSE 스트리밍 API
- 테스트 23개 통과

### 검증 결과

```text
uv run python -m pytest
23 passed, 2 warnings
```

확인한 URL:

```text
http://127.0.0.1:8000/
http://127.0.0.1:8000/health
http://127.0.0.1:8000/docs
```

### 확인된 이슈

- `.env`에 실제 Upstage, 기업마당, Supabase 키가 들어 있으므로 GitHub 업로드 전 반드시 제외해야 한다.
- LLM이 켜진 상태에서는 답변이 자연스러워지는 대신 출처 없는 기준 연도, 금액, 조건을 말할 위험이 있으므로 프롬프트/가드레일을 더 강화해야 한다.
- 기업마당 API 응답 필드는 실제 공고별로 비어 있을 수 있어 정규화 로직 보강이 필요하다.
- `_merge_backup_before_team_structure` 백업 폴더는 GitHub에 포함할지 제외할지 결정해야 한다. 일반적으로는 올리지 않는 편이 좋다.

### 다음 작업

- LLM 응답 가이드라인을 `app/core/prompts.py`에 더 엄격하게 반영
- LLM 사용 시 정책 후보 데이터 밖의 사실을 생성하지 않는지 수동 QA
- 기업마당 API 실제 응답 샘플 기반 필드 매핑 보강
- Docker Compose 실행 검증
- GitHub 업로드 전 `.gitignore`와 불필요한 백업 폴더 확인
- 발표용 데모 시나리오 2개 확정

### 멘토링 때 확인할 질문

- LLM을 정책 추천 응답 생성에 사용할 때 필요한 최소 가드레일이 충분한가?
- 기업마당 API 실패 시 빈 결과로 처리하는 구조가 평가 관점에서 적절한가?
- 정책 추천 점수화 기준이 발표 데모 수준에서 충분한가?
- 발표 데모는 청년/창업 2개 시나리오면 충분한가?
- 실제 API 데이터가 불완전할 때 빈 결과/안내 fallback을 어디까지 명시해야 하는가?

## 2026-07-10

### 오늘의 결정

MVP 핵심 대상을 취업 준비 청년으로 재확정했다. 대학생, 졸업예정자, 사회초년생, 미취업 청년을 우선 사용자로 두고, 취업 지원정책과 채용정보 추천을 중심으로 개발한다.

### 데이터 소스 방향

- 온통청년 Open API를 청년 취업정책의 핵심 데이터 소스로 신청한다.
- 고용24 국민내일배움카드 훈련과정 API를 실데이터 핵심으로 사용하고, 고용24 채용정보 API는 권한 허용 범위에서 보조로 사용한다.
- 이미 확보한 기업마당 API는 창업/사업자 관련 질문에 대응하는 보조 데이터로 유지한다.

### 문서 반영

- README의 MVP 설명을 취업 대상 청년 중심으로 수정했다.
- API 및 배포 메모에 온통청년, 고용24 훈련과정, 고용24 채용정보, 기업마당의 역할과 예상 환경변수를 정리했다.
- 개발 현황, 로드맵, 다음 작업 체크리스트에 API 신청과 필드 매핑 작업을 추가했다.

### 다음 작업

- 온통청년 Open API 인증키 신청
- 고용24 국민내일배움카드 훈련과정 API 인증키 신청
- 고용24 채용정보 API 개인키 조회 제한 확인
- 온통청년 JSON 응답을 내부 `YouthPolicyItem` 스키마로 매핑
- 채용정보 API는 제한 응답 시 채용 탐색 가이드로 폴백하도록 설계
- 취업 MVP 데모 질문 3개 확정

### Day3 개발 진행

- LLM 응답 프롬프트에 후보 데이터 밖의 정책명, 금액, 날짜, 자격 조건, 링크 생성 금지 규칙을 더 명확히 반영했다.
- 템플릿 응답도 추천 이유, 지원 대상, 신청 기간, 신청 방법, 신청 전 확인 필요 조건, 원문 링크가 항상 드러나도록 보강했다.
- `/api/chat/stream`이 답변 생성 전 `status` 이벤트를 먼저 보내고, 빈 답변이나 서버 오류는 SSE 오류 이벤트로 반환하도록 개선했다.
- 정적 채팅 UI에서 빈 말풍선 대신 진행 상태를 보여주고, 첫 토큰 수신 시 실제 답변으로 교체하도록 수정했다.
- 기업마당 API 정규화에서 `response > body > items > item` 형태와 일부 빈 필드를 안전하게 처리하도록 보강했다.
- 조건 부족 실패 케이스와 기업마당 필드 누락 케이스 회귀 테스트를 추가했다.

### Day3 검증 결과

```text
uv run python -m pytest
26 passed, 2 warnings
```

로컬 서버 검증:

```text
/health 200
/ 200
/docs 200
/api/chat/stream 200, event: token / event: done 확인
```

Docker Compose 설정은 유효하지만 Docker Desktop 데몬이 실행 중이지 않아 빌드는 확인하지 못했다. Docker Desktop 실행 후 `docker compose build`와 `docker compose up` 검증이 필요하다.

### Day3 개발 가이드 수정

- 오늘 핵심 MVP를 청년지원사업 및 취업 관련 청년 정보 챗봇으로 재정의했다.
- 현재 연결 상태를 기준으로 고용24 국민내일배움카드 훈련과정 API를 우선 실데이터 연동 대상으로 잡았다.
- 고용24 채용정보 API는 개인키 권한 제한을 전제로 채용행사/공채속보/공채기업정보 또는 채용 탐색 가이드 fallback 중심으로 정리했다.
- 온통청년 청년정책 API는 키 미설정 상태이므로 실제 호출을 오늘 blocker로 두지 않고, 키가 없으면 빈 결과로 처리하며 스키마/normalizer 준비 작업으로 분리했다.
- 기업마당 API는 오늘 핵심 MVP에서 제외하고 창업/사업자 질문의 보조 데이터로 유지하기로 했다.
- 발표자료와 발표용 데모 시나리오 확정은 오늘 개발 가이드에서 제외했다.

### Day3 revised 구현 진행

- `PolicySearchInput`에 나이, 졸업 상태, 관심 직무, 희망 지원 유형 필드를 보강했다.
- `YouthPolicySearchInput`, `TrainingCourseSearchInput`, `RecruitmentInfoSearchInput`과 각 출력 스키마를 추가했다.
- 고용24 국민내일배움카드 훈련과정 XML normalizer를 추가하고, 과정명/기관명/지역/기간/비용/NCS/상세 URL 매핑을 구현했다.
- 고용24 훈련과정 API는 기본 조회에서 결과가 반환되는 것을 확인했다. 특정 키워드 결과가 없을 때는 훈련과정 탐색 가이드로 fallback한다.
- 고용24 채용정보 API의 개인회원 권한 제한 문구를 감지하고, 직접 공고를 생성하지 않는 채용 탐색 가이드로 변환하도록 구현했다.
- 온통청년 키 미설정 상태에서는 내부 정책 데이터로 청년지원사업 fallback이 이어지도록 `YouthCenterRepository`를 추가했다.
- Agent에 `request_kind`를 추가해 청년지원사업, 훈련과정, 채용 보조 정보 흐름을 구분하도록 했다.
- 훈련과정/채용 fallback API 라우트 테스트와 normalizer 단위 테스트를 추가했다.

검증 결과:

```text
uv run python -m pytest
37 passed, 2 warnings
```

## 2026-07-13

### 통합 및 운영화 결과

- LLM 중심 Router, 단일 Conversation Node, Tool 단일 선택 구조를 `main`에 통합했다.
- 최근 대화·프로필·미완료 요청을 Supabase에 저장하고 다음 요청에서 복원하도록 구현했다.
- 온통청년, 고용24 훈련, 고용24 허용 채용 3종, 기업마당 Repository의 live 응답과 정규화 결과를 확인했다.
- 정책 분야별 부족 조건을 확인하고 후속 답변 뒤 원래 검색을 재개하는 멀티턴 흐름을 보강했다.
- 종료 정책 제외, 지역 필터, 구체 검색어 보존, 누락 필드 안내와 일반 텍스트 응답 정리를 반영했다.
- React 채팅 메시지 렌더링 오류를 수정하고 프런트엔드 변경도 CI를 실행하도록 경로 트리거를 보완했다.
- PR #8을 병합한 최신 main 커밋은 `a89d1e3`이다.

### 검증 결과

```text
Ruff lint: 통과
Ruff format: 40 files already formatted
pytest: 78 passed, 2 warnings
frontend: vite production build 통과
GitHub Actions main CI: 성공
GitHub Actions 후속 CD: 성공
GCE 배포 작업 내부 /api/health: 성공
```

### 남은 작업

- 외부 배포 URL의 `/`, `/api/health`, `/docs` 수동 확인
- 훈련·채용·창업 Agent 전체 경로와 grounded 응답 QA
- Router 평가 질문과 기대 결과 표 확장
- 질문 유형별 SSE 상태 문구 적용
- 발표용 핵심 시나리오와 장애 fallback 시나리오 확정
- 로그인 기반 세션 소유권, 대화 보존·삭제, 동시 요청 충돌 제어 설계

### Day5 사전 준비

- 5일차 안정화 범위로 Router 결과와 부족 조건에 따른 SSE 상태 문구를 추가했다.
- 청년정책, 고용24 훈련과정, 채용 보조정보, 기업마당, 일반 설명, 범위 밖 질문의 상태 문구를 구분했다.
- React 타이핑 영역이 백엔드 `status` 이벤트의 실제 문구를 표시하도록 연결했다.
- Agent 예외가 내부 오류를 노출하지 않고 안전한 SSE `error` 이벤트로 바뀌는 회귀 테스트를 추가했다.
- Day5 실행 TODO, Router·Agent 12개 평가표, 수업 중 직접 진행할 Langfuse 가이드를 작성했다.
- 사용자 로컬 점검에서 새로고침 시 React 메모리 상태와 임의 채팅 ID가 초기화되는 문제를 발견했다.
- 채팅 목록·메시지·정책 카드·활성 채팅을 버전형 로컬 저장소에서 복원하고 UUID를 백엔드 `session_id`로 유지하도록 수정했다.
- 브라우저 저장 전 민감정보 마스킹, 최근 채팅 20개·채팅별 메시지 50개 제한, 개별·전체 삭제를 추가했다.
- 실제 브라우저에서 단일·다중 채팅 새로고침, 새로고침 뒤 주거정책 후속 검색 재개, 개별·전체 삭제를 확인했다.
- 브라우저 검증 중 Supabase 세션 문맥 조회 `401`이 1회 기록되어, 화면 복원과 별개로 수업 중 서버 키·RLS 및 서버 재시작 복원을 다시 확인하기로 했다.

검증 결과:

```text
Ruff lint/format: 통과
pytest: 88 passed, 2 warnings
React production build: 통과
frontend persistence: 4 passed
.env Git ignore: 확인
```

## 2026-07-14

### 지역 검색·스코어링 개선

- 성남시를 경기·온통청년 법정코드 `41130`·기업마당 `경기` 태그로 변환하는 공통 지역 정규화기를 추가했다.
- 온통청년과 기업마당에서 정확 지역·전국 후보를 우선하고, 해당 후보가 하나도 없을 때만 가까운 시·도 참고 결과 최대 3건을 제공하도록 변경했다.
- 인접 결과에 `nearby_reference`, 대표 좌표 기준 직선거리, 거주 요건 확인 안내를 추가했다.
- 기업마당의 16개 공식 지역 태그와 `전남광주` 결합 태그를 정규화하고, 태그 누락을 전국으로 간주하던 추정을 제거했다.
- 점수 분모를 실제 확인 가능한 조건으로 계산하고 근거 충족률·하드 불일치·추천 가능 여부를 분리했다.
- 명시적인 지역·연령·취업·창업·사업자 등록 불일치와 만료 공고는 추천에서 제외했다.

### 로컬 UI에서 추가 발견·보완한 사항

- 온통청년 API가 의성군 지역사업에도 전국 시군구 `zipCd`를 넣는 사례를 확인했다.
- 등록·상위·주관기관의 공식 지방자치단체명을 교차 검증해 해당 사업을 경북 지역사업으로 분류했다.
- 사업 종료일이 없더라도 고정 신청기간이 2025년에 끝난 주거안정장학금은 현재 추천에서 제외했다.
- API의 `0~0세` 값을 실제 연령 범위가 아닌 `연령 제한 없음`으로 정규화했다.
- 최종 브라우저 질의에서 성남시와 무관한 의성군 사업, 만료 장학금, `0~0세` 문구가 모두 사라지고 국토교통부 전국 정책만 표시되는 것을 확인했다.

### 검증 결과

```text
Ruff lint: 통과
Ruff format: 43 files formatted 확인
pytest: 133 passed, 2 warnings
frontend persistence: 4 passed
frontend production build: 통과
온통청년 live: 성남시 주거 검색 1건(전국, 국토교통부)
기업마당 live: 경기 정확 결과가 전국 결과보다 우선함을 확인
로컬 UI: 성남시 지역 필터·만료 필터·연령 센티널 표시 확인
```

### 전국 시·군·구 확장

- 행정표준코드관리시스템의 2026-07-14 현존 시·군·구 300개와 공식 5자리 코드를 `administrative_regions.py`에 스냅샷으로 추가했다.
- 모든 시·군·구를 시·도 문맥과 함께 공식 코드로 복원하는 전수 회귀 테스트를 추가했다.
- 기업마당이 지원하는 16개 지역 태그에 대표 시·군·구를 연결하는 전수 테스트를 추가했다.
- 같은 이름이 여러 곳에 있는 `중구`, `고성군`은 시·도 없이 임의 선택하지 않도록 변경했다.
- 같은 도 안의 성남시와 수원시를 같은 지역으로 통과시키던 온통청년 범위 판정 문제를 5자리 코드 기준으로 수정했다.
- 프로필 추출 LLM이 `고성군`을 임의로 `강원 고성군`으로 바꾸더라도 현재 발화의 지역 표현으로 다시 덮어쓰도록 보강했다.
- 해운대구 live 조회에서 온통청년 `26350`, 기업마당 `부산` 태그를 확인했고 부산 정확 정책과 전국 정책만 반환됐다.
- 전주시 live 조회에서 온통청년 `52110`, 기업마당 `전북` 태그를 확인했다.
- 로컬 UI에서 `고성군` 시·도 확인 질문과 후속 `경남 고성군이야` 뒤 원래 주거 검색 재개를 확인했다.

### 기업마당 지역·자격·점수 교차 검증

- 성남 `경기`, 해운대구 `부산`, 전주 `전북` 태그로 실제 API가 호출되는지 다시 확인했다.
- 기업마당이 일부 지자체 제한 공고에도 모든 지역 태그를 붙이는 사례를 발견해, 공고 본문의 소재지·본사 이전·전입 조건을 우선하는 교차 검증을 추가했다.
- 응답의 전국 태그가 `전남광주` 결합형 16개와 `광주`·`전남` 분리형 17개로 나뉘는 사례를 모두 전국으로 정규화했다.
- `지역제한 없음` 문구는 지역 태그와 지자체 기관명보다 우선해 전국 대상으로 판정한다.
- `예비창업자`, `창업 N년`, `업력`, `사업자등록` 문구를 구조화하고 사업자 미등록 사용자에게 기창업자 전용 공고가 추천되지 않도록 했다.
- 기업마당 조회 폭을 요청 건수의 최대 5배로 확장하고, 요식업↔농식품 등 관심 분야 유사어를 우선 정렬했다.
- 동적 분모 때문에 근거가 적은 공고도 100점이 되던 계산을 제거하고, 전체 가중치 대비 추천 적합도와 실제 비교 범위인 근거 확인률을 분리했다.
- 관심 분야 가중치를 20%로 조정해 지역·자격만 맞는 무관한 공고보다 질문 주제에 맞는 공고가 먼저 오도록 했다.
- 정책 카드에 `요청 지역 일치`·`전국 대상`·`인접 지역 참고` 배지와 추천 적합도·근거 확인률을 표시했다.
- 부산 해운대구 카페 예비창업자 브라우저 질의에서 타 지역 제한·기창업자 전용 공고가 제외되고 `농식품 크라우드펀딩`이 68점으로 1순위에 표시됐다.
- 새로고침 뒤에도 68점과 전국 대상 배지 5개가 복원되는 것을 확인했다.

검증 결과:

```text
Ruff lint/format: 통과
pytest: 143 passed, 2 warnings
frontend persistence: 5 passed
frontend production build: 통과
기업마당 live: 성남=경기, 해운대구=부산, 전주=전북 요청 태그 확인
로컬 UI: 부산 카페 예비창업자 관련성·사업자조건·전국범위·점수·새로고침 확인
```

### 새 채팅 프로필 재사용·온통청년 장애 구분

- `청년 주거 지원 정책에 대해 알려줘`가 `SEARCH / explain`으로 분류되면서 지역·나이 확인을 건너뛰던 원인을 수정했다.
- 광범위한 현재 정책 목록 요청은 `SEARCH / recommend`로 분류하고, 특정 정책명의 개념·자격 설명만 explain으로 처리하도록 Router 예시를 보강했다.
- 거주 지역과 만 나이를 브라우저 기본 프로필로 별도 저장해 새 채팅 요청에 전달하고, 같은 채팅의 Supabase 프로필을 우선하도록 했다.
- 전체 로컬 기록 삭제 시 기본 프로필도 함께 삭제하도록 했다.
- 온통청년 요청의 브라우저형 헤더를 제거하고 5xx를 한 번 재시도한 뒤, 모든 호출이 실패하면 `결과 없음`이 아니라 공식 API 조회 장애로 안내하도록 분리했다.
- 경기·만 24세·주거 live Repository 조회에서 전국 대상 1건을 확인했다.
- 로컬 API와 실제 UI에서 기존 지역·나이가 새 채팅에 적용되고 조건 재질문 없이 주거 정책을 반환하는 것을 확인했다.

검증 결과:

```text
Ruff lint/format: 통과
pytest: 149 passed, 2 warnings
frontend persistence/profile defaults: 7 passed
frontend production build: 통과
온통청년 live: 경기·만 24세·주거 1건(전국), 장애 안내 아님
로컬 UI: 경남 고성군·만 25세 조건을 새 채팅에서 재사용해 주거 정책 추천
```
